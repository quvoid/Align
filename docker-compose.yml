version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: branddeals-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: branddeals
      POSTGRES_PASSWORD: branddeals
      POSTGRES_DB: branddeals
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U branddeals"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: branddeals-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy volatile-lru

volumes:
  postgres-data:
  redis-data:
